# 项目进度

更新日期：2026-09-16。应用 **0.6.4**；Hermes Inky Coach 插件 **0.1.3**。本文记录已实现能力、已有验证和后续检查，不把历史验收当作本轮重跑结果。

## P0/P1：准备0.7.0-beta.1试用交付，完整评测后移

本轮以 `d13b19f` 为基线，在 `codex/inky-workbench` 按工作包推进，M0—M4 已放行。共同队列与字段、旧安排处理、准备来源、计时保护、可确认调整均复用同一数据层；M3补充草稿恢复、批量改期、笔记整理和轻量收尾；M4补充预算、项目与可纠正偏好。一次集成检查255项前端、152项Rust通过；后续模型状态与本地时刻修复另有专项检查。完整最终检查留到M5，不能把中途数字当最终版本结论。

原生桌面验证使用独立数据，包括完整重启、真实提交后丢响应的原请求重试、笔记来源、父任务保留、时间/项目/偏好操作。M4实际模型遇到连接失败及过去钟点建议，分别修复状态归类和当前本地时间后重跑；真实5分钟偏好已进入候选字段，正式计划和时钟未被自动改变。见 [M1](verification/p0p1/M1/README.md)、[M2](verification/p0p1/M2/README.md)、[M3](verification/p0p1/M3/README.md)、[M4](verification/p0p1/M4/README.md) 和 [工作包台账](plans/p0p1/ledger.md)。

用户随后要求先交付试用版，完整Coach固定案例评测后移。本次源码版本为0.7.0-beta.1，类型检查、273项前端、170项Rust（1项性能基准忽略）及MCP schema检查通过，正在构建交付。C03真实桌面/模型及剩余完整验收仍待办，不宣称P0/P1全部验收完成。见 [试用版交付记录](verification/0.7.0-beta.1/README.md) 和 [M5未完成项](plans/p0p1/M5_PENDING.md)。下方版本记录是历史交付事实。


## 0.6.4 纸面与工作台集成交付

将纸面拖动与浮动滚动条提交 `2d0d8a3` 合并进已交付工作台分支 `1dfeef6`。保留 0.6.3 的日期、讨论范围、返回 Inky 入口和此前每日记录 / Markdown；新增固定纸边拖动与不占位细滚动条。产品代码自动合并，三份说明文档的冲突已合并保留两边记录。

本轮类型检查、122 项前端测试、70 项 Rust 测试通过，1 项历史负载基准忽略；release 构建通过。真实独立桌面验收覆盖滚动与拖窗、任务划线、透明与迷你宠物，以及工作台选步骤回到 Inky、结束页完成记录回流、跨日和未安排工作、个人 Markdown 外部编辑与 1060px 布局。本轮没有调用真实 Hermes 模型，模型能力的历史验证见 0.6.3。详见 [0.6.4 验收与交付](verification/0.6.4/README.md)。

合并后的可执行文件已更新到桌面快捷方式对应的原路径，并启动 0.6.4；旧文件已备份。源码统一进入 `main`，仓库已按用户要求改为 **PUBLIC**，版本下载见 [GitHub 0.6.4](https://github.com/Xuzheee/Inky-Paper/releases/tag/v0.6.4)。

## 纸面分支：滚动后的拖动与浮动滚动条

纸张左侧 10px 边缘和底部 6px 留白固定提供拖动，不依赖随列表滚走的页眉。首页及各纸面主滚动区采用 3px 浮动滑块，透明抓取区宽 10px，滚动 / 悬停 / 键盘聚焦显示，闲置淡出，不占内容宽度。任务横划与表单操作保持独立，迷你宠物卸载这层控件。

原纸面分支的类型检查、114 项前端测试与 release 构建通过。独立 Tauri / WebView2 检查覆盖顶部 / 中部 / 底部拖动、零滚动条占位、滑块拖动 / 滚轮 / 键盘、大小任务划线、记录输入及保存、页面切换、透明态和迷你宠物；见 [拖动与滚动验收](verification/paper-chrome-20260916/README.md)。这部分已合并至 0.6.4；本次集成验证与交付见版本记录。

## 0.6.3 日期、讨论范围与返回 Inky

候选卡片保留每次请求的安排日期，采用按钮显示目标日；手动修改和不确定请求重试保留原日期与请求参数。旧对话没有日期时要求用户选择，不猜测今天。对话保存发送时的日期与任务范围，顶部区分当前回复与下次发送；切换日期、查看记录或取消选择后清除旧任务范围。

任务详情和已采用卡片新增「设为下一步并回到 Inky」：使用现有版本化选择操作，主窗口选中对应任务、步骤和时长，start 仍由用户点击。进行中的番茄钟会阻止替换步骤；普通返回入口可回到列表并保留计时。

类型检查、112 项前端测试、70 项 Rust 测试通过，1 项历史负载基准忽略。真实 Tauri 隔离验收覆盖跨日范围、回复途中切日、真实 Hermes 候选与采用、两个返回入口、活动计时保护、刷新和完整重启。最终界面再次复验已采用卡片不显示重复核对。便签字体与番茄钟 90% 缩放通过 Git 合并包含在本版。详见 [0.6.3 验收](verification/0.6.3/README.md)。

## 0.6.2 工作台每日记录与 Markdown

工作台新增「每日记录」与「Markdown」页签，按日期浏览同一 Paper 数据库中的计划、番茄钟、完成与撤销、工作时段进展、随手记和已有总结。日期列增加完成步骤与计时摘要，日程显示完成标记。区分计划的当前完成状态与当天实际确认完成的步骤，跨日完成按结束日统计；工作与休息分开计时。

Markdown 页直接读取已有的每日原文件、个人复盘和全部任务文件，保留原文，不重新拼接或写回任务。支持打开原文件；个人复盘外部改动自动重新读取，空白日期不生成虚假记录。后端只接受固定文件类型与严格日期，超大文件保留外部打开入口。

类型检查、107 项前端测试、68 项 Rust 测试通过，1 项历史负载基准忽略。真实 Tauri 隔离验收覆盖跨窗口完成与撤销同步、未安排工作、按日查看、原文与磁盘一致、个人复盘外部修改、空白日期和 1060px 布局。此轮没有触发真实模型请求；详情见 [0.6.2 验收](verification/0.6.2/README.md)。

## 纸面分支：便签字体与更小的番茄钟

首页便签的任务文字改为 Xiaolai 20px，保留绿色纸卡和清单样式。番茄钟以原横向构图整体缩小 10%，正常约 360×189、暂停 360×274、记录 360×359；文字、按钮、宠物和间距同步缩放，透明态保持相同位置。结束页、休息页及迷你宠物尺寸不变。

原分支类型检查、104 项前端测试和 Rust 窗口布局专项通过。独立 Tauri / WebView2 验证实际字体、短/长便签、纸面/透明态、深浅背景、拖动、暂停、记录、键盘、长任务与三位分钟、返回列表及迷你宠物恢复。见 [字体和缩放验收](verification/compact-focus-20260915/README.md)。现已合并至 0.6.3；本轮复验了便签显示和工作台返回后的手动开始，未重复上述全部历史视觉检查。

## 纸面分支：结束页排版与保存说明

`codex/markdown-coach` 在 0.6.1 基础上完成：手写任务固定 126px 高，按实际排版在 42–18px 间缩放，极长内容可查看全文；结束页固定 320×528，展开记录在页内滚动；纸面滚动条改为 4px，首页、结束页及休息页收紧底部留白，休息窗口为 320×430。

完成选项下直接说明保存去向：未完成回列表并保留步骤；已完成划掉当前步骤，再由用户选择回列表或休息。选择本身仅保存草稿，整个任务独立完成，返回番茄钟不会自动恢复计时。

纸面分支的类型检查、104 项前端测试、67 项 Rust 测试通过，1 项历史负载基准忽略。隔离 Tauri / WebView2 验证覆盖短任务、长中文和 300 字中英文换行文本、真实字体、全文阅读、拖动、选项、草稿恢复、两种保存结果、休息与 Markdown 回流。历史证据见 [结束页验收](verification/end-layout-20260915/README.md)。

现已通过 Git 合并进 0.6.2；合并版本的 107 项前端、68 项 Rust 测试通过，并复验了固定 320×528 结束页保存后，步骤完成与产出同步到工作台每日记录和原始 Markdown，父任务仍保持独立。两个分支的更新统一包含在本机 `app/Inky Paper.exe`，交付校验见 [0.6.2 验收](verification/0.6.2/README.md)。

## 0.6.1 首页工作台入口

首页底部增加「工作台」，与「随手记 · 足迹」并列，点击直接打开独立工作台。沿用原有导航样式，宠物与英文标语保持原位。修复 Windows 首次从同步命令创建 WebView2 窗口时卡住的问题：页面命令和托盘 / 单实例回调改为异步打开，启动时仍使用 setup 创建路径。

在隔离的真实 Tauri 窗口验证了并列位置、首次点击加载、重复点击复用窗口及打开前后任务 / 计划 / 计时状态相同。类型检查、104 项前端测试和 67 项 Rust 测试通过，1 项历史负载基准忽略。最终可执行文件与交付校验见 [0.6.1 验收](verification/0.6.1/README.md)。

## 0.6.0 独立工作台

依据选定的 Figma 页面，新增左侧月历、中间三日任务 / 日程、右侧 Coach 对话的独立窗口。左上角为 Inky，托盘「打开工作台」或 `启动 Inky 工作台.cmd` 进入；原有纸面任务与专注界面保持原流程。

可新增和修改任务 / 步骤、分类筛选、拖动跨日和排序、编辑日程时刻与时长、手动完成步骤、选择下一步。工作台使用同一 Paper SQLite 和版本化命名操作。日程字段兼容旧数据，改计划不改写执行快照。

Hermes ACP 已接入真实对话、候选卡片编辑和采用、历史恢复、续聊、取消。工作台进程过滤全局 MCP 配置，显式连接自身的 Paper 工具；Hermes 模型、账户和记忆提供方沿用原配置。采用只由用户点击，模型仍不控制计时。自定义项目、附件、第三方日历同步、工作台内模型设置尚未实现。

本轮类型检查、前端 104 项与 Rust 67 项通过，1 项历史负载基准忽略。隔离 Tauri 验证覆盖任务共用、步骤完成独立、跨日拖动、日程、下一步、真实模型读取与候选生成、手动采用、刷新和重启恢复、真实续聊、取消、时间格预填与并发编辑冲突。最小 1060px 窗口无横向溢出。未将这些检查扩展成全部专注流程的桌面复验。

0.6.0 release 已同步到 `app/Inky Paper.exe`，版本和 SHA-256 一致；旧版已备份，用户原有进程保持运行，需正常退出再打开以加载新版。见 [使用说明](工作台使用说明.md)、[本轮验收](verification/0.6.0/README.md) 和 [交付校验](verification/0.6.0/release-verification.json)。

## 当前已实现

| 部分 | 当前行为 |
| --- | --- |
| 计划与 Coach | 用户询问才读取记录、建议拆分或保存总结；Hermes 展示可选择、编辑和排序的真实卡片，点击采用后加入日计划 |
| 纸面执行 | 大任务展开步骤；Do this 选入便签，start 才计时；简单任务去重；父任务与步骤均可手动完成，保留原位且完成状态独立 |
| 番茄钟与结束页 | 返回列表保留计时；结束入口先暂停；补充记录、完成选项作为会话草稿，单个“保存并结束”提交；支持滚动、纸面拖动和休息 |
| 手写视觉 | 结束页任务使用 Xiaolai 42–18px 自适应字号，选项使用 Yozai Regular 18px，随包提供字体与 OFL；铅笔涂抹选中动画，鼠标与键盘均可操作；共用英文随机标语 |
| 操作提醒 | 保存、结束休息、下一步变化等反馈使用独立 300×156 纸面小窗，靠近主窗口且不抢焦点；知道了 / 关闭 / Esc / 8 秒收起，连续提醒更新同一窗口 |
| 共同记录 | SQLite 命名事务、版本校验与重复请求保护；计时快照保留；自动导出 Markdown，个人笔记可手改；手动完成与撤销单独记录，不补造时长 |

Coach 不因记录更新或到点自动介入，卡片采用和时钟控制保留给用户。保存总结会校验读取时的数据与笔记版本；出现新记录后，历史总结保留并提示已变化，等待用户再次询问。

## 0.5.11 已选横向纸条

用户选择第 1 版，已接入真实应用。正常态 400×210，手写任务在左、计时在右，下方保留铅笔进度、暂停、鱼与随手记。暂停展开为 400×304，记录 / 工作菜单为 400×398。正常与透明态计时、进度坐标不变；透明态采用浅色笔画和细深色描边，返回 / 结束入口保持可用。任务、步骤、计时和 Coach 数据规则不变。

类型检查、前端 102 项（7 个文件）、Rust 65 项及 release 构建通过；1 项历史负载基准忽略。隔离 Tauri 验证覆盖真实拖动、10 秒淡出、深浅背景、暂停继续、记录输入、键盘和减少动画、返回保存、长标题与三位分钟、迷你宠物、工作菜单和等待 AI。首轮发现暂停页多余滚动与字号 / 进度偏差，修正后复验通过。见 [视觉验收](../design-qa.md)、[同尺寸对照](verification/0.5.11/comparison.png) 和 [桌面结果](verification/0.5.11/desktop-verification.json)。

0.5.11 已交付到 `app/Inky Paper.exe`，与 release 版本、哈希一致，桌面快捷方式保持原路径；[交付校验](verification/0.5.11/release-verification.json)。日常进程未中断，需从托盘退出后重开。两个测试进程均已关闭，测试数据留在 Git 排除的 `output/`，不再重试已被拒绝的临时目录删除。此次未重新调用 Hermes 模型或检查无关页面的全部视觉。

## 0.5.10 番茄钟透明显示修复

原因是后加的统一样式在透明态分别给导航和计时容器铺上 96% 不透明底色，覆盖了原来的纸面淡出设计。本次删除这两层底色和计时容器投影，将深浅背景的可读性处理放回文字 / 铅笔笔画；计时和进度在淡出前后坐标不变，返回与结束入口保持可用。

类型检查、前端 102 项（7 个文件）和 release 构建通过。隔离 Tauri / WebView2 检查通过：真实等待进入透明态、没有容器底色与溢出、计时继续、鼠标 / 键盘恢复、键盘焦点保留、暂停继续、随手记编辑、减少动画、返回列表保留计时、结束保存不完成父任务。真实渲染检查覆盖纸面、透明、深色、浅色和混合背景。仅修改 CSS，没有后端业务行为变更，因此未重跑 Rust 全套。证据见 [桌面检查](verification/0.5.10/desktop-verification.json)、[纸面](verification/0.5.10/paper.png)、[透明](verification/0.5.10/transparent.png)、[深色](verification/0.5.10/dark.png)、[浅色](verification/0.5.10/light.png)。

0.5.10 已交付至 `app/Inky Paper.exe`，版本和 SHA-256 与 release 一致，桌面快捷方式仍指向该路径。日常进程未被中断，从托盘退出再打开才会加载修复；[交付校验](verification/0.5.10/release-verification.json)。独立测试进程已停止，报告之外的测试资料继续留在被 Git 排除的 `output/`，不再重试此前被拒绝的删除操作。

另生成了 [三版番茄钟设计原型](design/focus-options-20260915/README.md)。0.5.10 当时只包含旧界面的透明修复，后来用户选择第 1 版，已在 0.5.11 接入。

## 0.5.9 独立提醒窗口

将原先占据首页空间的四类操作反馈移到独立原生小窗。提醒只保存临时展示状态，显示与关闭不更改任务、计时或 Coach 记录。表单错误和需要用户操作的上下文提示仍留在所在页面。

本次前端 102 项通过（7 个文件），Rust 65 项通过、1 项历史负载基准忽略，类型检查通过。独立调试数据中的真实 Tauri / WebView2 验收通过：保存未完成、结束休息、主窗口与弹窗 Esc、知道了与关闭按钮、不抢焦点、连续提醒替换与过期保护、8 秒自动关闭、窗口重载恢复，以及提醒操作不改任务和计时。

证据见 [桌面检查](verification/0.5.9/desktop-verification.json)、[实际弹窗](verification/0.5.9/saved-notice.png) 和 [任务页](verification/0.5.9/task-page.png)。未重新运行 Hermes 模型或完整结束页视觉验收，也未向日常数据库写入测试任务。

release 构建完成并交付到 `app/Inky Paper.exe`，版本 0.5.9，文件 67,661,312 字节，SHA-256 与构建产物相同。桌面快捷方式指向此文件；运行中的旧版未被中断，须从托盘退出再打开。旧 0.5.8 已保存在 `app/backups/`。详见 [交付校验](verification/0.5.9/release-verification.json)。

隔离测试进程已停止。本轮 `output/notice-window-20260915/` 和 `output/playwright/notice-window-20260915/` 的临时数据删除被自动审批以 `blocked by policy` 拒绝，暂时保留并继续由 Git 排除；正式验证报告与截图已存入上面的版本目录。

## 历史验证依据

| 日期 / 版本 | 已检查范围 | 证据 |
| --- | --- | --- |
| 2026-09-13 / 0.5.0 | 每日计划、执行与 Markdown 核心闭环；重启恢复；规划模型调用 | [桌面](verification/0.5.0/desktop-verification.json)、[重启](verification/0.5.0/restart-verification.json)、[规划模型](verification/0.5.0/planning-model-verification.json) |
| 2026-09-14 / 插件 0.1.1 | 独立 Hermes 桌面和 Paper 测试库：六张候选中选两张、编辑、排序、明确采用后写入；此项未运行真实模型 | [卡片验收](verification/hermes-0.1.1/ui-acceptance.json) |
| 2026-09-14 / 0.5.6–0.5.7 | 0.5.6 Rust 61 项通过、1 项基准忽略；0.5.7 前端 128 项通过。此处是历史基线 | [0.5.8 交付报告中的基线记录](verification/0.5.8/release-verification.json) |
| 2026-09-14 / 0.5.8 | 类型检查、构建和隔离 Tauri 桌面：实际字体、窗口拖动、键盘与减少动画、草稿保留、保存结果、父步骤分离、长内容滚动、休息和 Markdown | [桌面检查](verification/0.5.8/desktop-verification.json)、[实际字体](verification/0.5.8/rendered-fonts.json)、[字体文件](verification/0.5.8/font-verification.json)、[交付报告](verification/0.5.8/release-verification.json) |

0.5.8 当时未重跑前端与 Rust 全套测试，引用了上表基线；未重测完整退出，也未强制重启日常应用或向日常数据库写入测试内容。Hermes 插件 0.1.2–0.1.3 加入简短文案和手动记录规则，不能据此声称所有真实模型回答均已验收。

历史 0.5.8 交付 SHA-256（本轮重建后的校验另行记录）：

```text
11CD6EF20CF6CE7E9D6F235AB66B207EFEBF62EC8FD52B0B66D91E976F9C25DA
```

可查看 [结束页](verification/0.5.8/end-default.png)、[选中状态](verification/0.5.8/end-shading-selected.png)、[展开记录](verification/0.5.8/end-expanded.png)、[长任务滚动](verification/0.5.8/long-task-scroll.png)、[休息中](verification/0.5.8/rest-running.png)、[休息结束](verification/0.5.8/rest-waiting.png)。这些截图来自对应版本的隔离验收。

## 0.5.8 项目整理记录

本轮统一说明入口，将未挂载的旧 Inky 代码、重复交付物、测试数据库、临时配置和构建缓存移出维护目录，保留当前运行源码、必要素材、许可及代表性验收证据。旧 PRD 和脚本归档供追溯。此次整理不增加功能，不产生新的界面质量结论。

永久删除操作被自动审批拒绝，因此使用项目内 `.cleanup-quarantine/` 做可逆隔离，并从 Git 排除。隔离不等于永久删除，也没有释放对应磁盘空间。

2026-09-15 整理后：类型检查、release 构建通过；当前前端 98 项通过（6 个测试文件），Rust 61 项通过及 1 项基准忽略，Hermes 插件前端 6 项、Python 7 项、MCP 12 个工具检查通过。旧代码的 30 项测试随旧模块隔离，前端测试命令明确限定 `src`。本轮未重跑真实桌面，不新增视觉验收结论。

已将重建的 0.5.8 交付到 `app/Inky Paper.exe`，文件为 67,342,336 字节，比之前减少约 3.82 MiB；前端 JS / CSS 与此前产物哈希一致。重建 SHA-256：

```text
8B16D4946B193F53A83112195F7D6B645882D52D99650484E0FFE8109E88F577
```

本轮检查记录见 [整理验证](verification/cleanup-20260915/verification.json)。共将 38 个目标、33,880 个文件（约 24.19 GiB）移入 `.cleanup-quarantine/20260915/`，包括旧源码、旧程序副本、测试产物和构建缓存；[完整清单](verification/cleanup-20260915/quarantined-files.json) 与 [统计](verification/cleanup-20260915/cleanup-summary.json) 已保存。当前程序与一个 0.5.7 回退副本保留在 `app/`。隔离目录永久删除仍未完成。

Git 已建立并推送至 [Xuzheee/Inky-Paper](https://github.com/Xuzheee/Inky-Paper)，首次建立时为 **PRIVATE**，0.6.4 交付时按用户要求改为 **PUBLIC**。默认分支 `main`，本地跟踪 `origin/main`。首次源码基线提交为 `2c76c15`；后续提交可在仓库历史中查看。当时源码约 89.78 MiB，最大单文件小于 GitHub 的单文件限制；入库检查未发现所检查格式的凭据或禁止上传的运行产物。

`.gitignore` 排除正式数据库、工作记录、本机连接、依赖、构建输出、exe 和隔离目录。当前记录并不会因 Git 提交而自动同步到 GitHub。

## 仍需检查与后续范围

当前人工检查以 [MANUAL_CHECKS.md](MANUAL_CHECKS.md) 为准，重点是用户实际桌面版本、日常 Hermes 的两个插件开关、短标题生成质量和完整闭环的操作手感。

工作时段总览、工作收尾、随手记及历史空状态等辅助页面，尚没有与本次结束页同等范围的最新视觉验收。后续可逐页检查比例、留白、导航和页脚，不把静态审阅视为已完成的界面验收。

当前交付是本地 Windows 应用和源码，尚未覆盖长期大数据量使用、其他平台、自动更新分发和实际专注 / 产出改善效果。是否继续这些方向由真实使用反馈决定。
