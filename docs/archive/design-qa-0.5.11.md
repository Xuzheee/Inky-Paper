> 历史0.5.11视觉验收，文中尺寸不是当前90%缩放尺寸。原文路径以仓库根目录为基准。当前状态见 [项目状态](../PROJECT_STATUS.md)。

# 横向纸条验收

final result: passed

已选视觉：`docs/design/focus-options-20260915/01-horizontal.png`。
最终实现截图：`docs/verification/0.5.11/paper.png`、`paused.png`、`note.png`、`long-task.png`、`dark.png`、`light.png`；同屏组合对照：`docs/verification/0.5.11/comparison.png`。

原图 1536×1024，操作态内容裁切区约 (139,435,534,280)，比较页以约 0.749 倍显示为 400×210；Tauri 实际 CSS 窗口 400×210、系统缩放 1.5，截图 600×315 在比较页按 400×210 显示。暂停态单独检查，未拿它与正常态做密度比较。组合图能清晰读出字体、按钮和进度，无需额外局部放大。

第一轮发现以下 P2，已修正并重新截图比较：

- 暂停页底部多余滚动，导致横向对齐改变。窗口 292→304；最终原生无溢出断言通过，截图没有多余滚动条。
- 数字、任务与鱼偏小，进度偏低。任务 22→24、数字 46→52、鱼 32→40，主要内容区 80→70 并调整底部间距。最终同尺寸组合图确认比例和留白已修正。

保持的实现约束：任务字体使用用户此前选定且已打包的 Xiaolai，数字使用现有 DM Sans，图标 / 纸纹 / 鱼 / 铅笔沿用正式素材。原型的静态透明图让数字居中，实际保持数字在右侧原位以防淡出时跳动；属于明确的稳定性约束。计时与进度来自真实测试会话，原型的中段进度不作为固定 UI 装饰复制。

最终五项检查：

| 检查面 | 结果 |
| --- | --- |
| 字体与层级 | 实际加载打包 Xiaolai 24px / 400 字重，两行上限；数字 DM Sans 52px，三位分钟为 42px。字体未回退，任务、数字、状态层级清晰。 |
| 比例与间距 | 400×210 横向布局；任务在左、计时在右，底部暂停 / 鱼 / 随手记。正常与透明数字 / 进度坐标相同，暂停 400×304、记录 400×398 和长任务均无溢出。 |
| 颜色与透明 | 沿用纸面、灰绿与墨色。透明态容器无底色，浅色笔画配细深色描边；已检查深色、浅色和混合背景。 |
| 素材质量 | 纸纹、鱼、加号、箭头和铅笔复用正式素材与组件，无整张原型覆盖，无拉伸和矩形残留底色。 |
| 文案与状态 | 返回任务列表、结束番茄钟、正在专注、暂停 / 继续、随手记均对应实际操作。暂停显示已暂停，等待 AI 单独表达。 |

交互验证：隔离 Tauri / WebView2 通过原生拖动、10 秒淡出、鼠标与键盘唤醒、键盘焦点保留、减少动画、暂停继续、随手记保存、返回列表保留计时、结束保存、长任务和三位分钟、迷你宠物往返、工作菜单、等待 AI / 回来继续、输入时不拖动窗口。主要流程无页面脚本错误。见 `docs/verification/0.5.11/desktop-verification.json`。

无未解决 P0 / P1 / P2。原型生成笔迹与真实字体笔画存在轻微 P3 风格差异，保留用户既有的随包字体；不影响阅读和操作。未覆盖 Hermes 模型与无关页面的完整视觉，不将当前局部验收外推。
