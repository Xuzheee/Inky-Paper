# P0/P1 暂停点与未完成项

## 最新安排：先交付试用版

用户随后明确要求先交付试用版，完整 Coach 案例评测后移。本次按 `0.7.0-beta.1` 构建并交付，执行必要自动检查；不运行固定 Coach 案例，也不将尚未进行的最终桌面/模型验收标为通过。交付状态以 [试用版记录](../../verification/0.7.0-beta.1/README.md) 为准。

下面是暂停时的历史快照；其中“未构建/未交付/版本0.6.4”描述当时状态。C03真实桌面、完整Coach案例及剩余M5验收继续待办，不阻塞此次用户明确要求的试用交付。

记录日期：2026-09-16。按用户最新要求：**先跳过 M5，待用户确认后再进行真实验证。** 本次只保存进度，不继续实现、启动桌面/模型、构建或更新交付程序。

## 当前状态

- 工作目录：`C:\Users\31009\clauddd\Inky-Paper`；分支：`codex/inky-workbench`。
- M0—M4 已按工作包完成，既有本轮证据保留在 `docs/verification/p0p1/M0` 至 `M4`。M4 验收提交为 `e62c473`，C05 采集器基础提交为 `8397602`。
- M5 的部分实现与脚本已经写入，保存为**未验收检查点**，不代表 M5 或 P1 全部完成。
- M5 真实桌面脚本、实际 ACP inventory 和固定模型案例均未执行；这些不沿用 M1—M4 的结果冒充通过。
- 未构建或同步新版 `app/Inky Paper.exe`，交付程序仍为此前的0.6.4。临时预升的源码版本号已恢复0.6.4，最终版本号留待交付时确定。
- 子任务均已停止；本轮没有仍在运行的 M5 测试、桌面或模型进程。没有访问或修改正式 Paper 数据库、旧版 Inky-app、正式连接或全局 Hermes 配置。

## 已写入，待集成审阅与真实验收

| 部分 | 已实现/准备 | 未完成 |
| --- | --- | --- |
| C-03 有依据总结 | 六类来源快照；真实读取凭据；数据/个人笔记双版本；原读取时刻计时；幂等保存；旧总结兼容；Markdown 依据导出 | 用真实模型检查正文与依据是否相符，验证保存冲突后重读、更新和多日回顾 |
| 总结界面与下一步 | 查看来源、过时提示、继续原步骤并回 Paper、真实今天仅预填候选 | 原生确认来源展示、当前/历史状态差异、双窗口交接和无自动计时 |
| 已处理来源保护 | UI 与后端拒绝继续已经处理的旧安排，保留旧总结和会话来源 | 两个新增后端保护尚未纳入最后一次完整 Rust 回归与原生验证 |
| 时长恢复 | 修复标题先恢复、时长晚一拍的中间状态；普通刷新保留用户手选时长 | 原生重启与启动时长回归 |
| C-05 采集器 | 24固定案例、6个指定重复样本；日期适配；独立测试库；工具/回答/状态采集；真实冲突和丢响应注入脚本 | 实际执行全部案例、逐项语义评价、红线复核与必要修复重跑 |
| ACP 探针清理 | Windows Job 限定本探针进程树；合成子进程测试通过 | 集成审阅和真实 ACP inventory 退出后清理验证 |
| 使用/恢复说明 | 工作台使用说明、数据备份与回退说明初稿 | 对最终实现和交付版本核对，完成发布记录 |

关键文件：`src-tauri/src/summary_evidence.rs`、`summary_evidence_tests.rs`、`src/workbench/SummaryEvidence.tsx`、`dailyRecord.ts`、`integrations/inky-paper-mcp-server/index.mjs`、`integrations/inky-coach-hermes-plugin/skills/coach/SKILL.md`。相关公共入口为 `paper.rs`、`paper_planning.rs`、`paper_markdown.rs`、`workbench.rs`。

审阅时还应核对两处已识别的小项：MCP 的 nextStart 说明应准确表达“保存建议，不准备/选择步骤”；旧会话依据如为 `legacy_start_date`，界面是否需要沿用每日记录的精度说明。此处只是待办，暂停后未继续修改。

## 暂停前实际完成的自动检查

- 最新完整前端：**273/273**；Paper 专项71/71；typecheck通过。新增恢复测试确定性复现旧25→18中间提交，修复后通过。
- 总结界面专项：17/17，包含同日不同个人笔记原句和已处理旧安排拒绝。
- 最后一次完整 Rust：**168通过、1忽略**；其后新增旧安排保护专项2/2、总结来源专项16/16通过。最后局部修复后未重跑完整Rust，不将168当作最终全量结论。
- MCP真实stdio schema1通过；Coach插件6通过；固定案例/日期适配/采集辅助26通过。
- 探针清理仅用合成 Windows 子进程测试3/3通过；没有启动 Hermes、桌面或模型。
- C03与M5原生脚本只有静态/语法检查。**脚本写好不等于原生验收通过。**

上述检查运行于隔离、合成数据。版本号回退只是取消提前发布标识，暂停后没有额外重跑测试。详见 [C03实现记录](../../verification/p0p1/M5/C03-implementation.md)。

## 用户确认后，按此顺序恢复

1. 重新检查 cwd、分支、未提交改动和暂停检查点；阅读本文件及公共 [contract.md](contract.md)。只审阅和补齐当前 M5 范围，不重写应用。
2. 先完成已写改动的集成审阅和必要自动检查，尤其最后追加的来源保护与 ACP 探针清理。重新构建独立 debug 包，记录提交和 SHA-256。
3. 用全新 `output/p0p1-m5-*` 数据目录分别执行 `scripts/desktop/verify-p0p1-c03.mjs`、`verify-p0p1-m5-regression.mjs`。不得直接对正式库运行。
4. C05 先跑 PLAN-01、ADJUST-04、REVIEW-05 冒烟，再执行24固定案例和6个指定重复样本。多轮前置场景会额外调用模型，记录实际次数。说明见 [eval-run-plan.md](../../verification/p0p1/M5/eval-run-plan.md)。
5. 对实际可见回答、工具输入/结果、事实版本、采用前后状态逐项评价。越权、编造已保存、错误日期写入、重复创建、未确认采用、历史改写任一出现都阻塞交付；保留失败样本，修复后新样本重跑。采集成功或结构校验不能替代语义通过。
6. 补齐最终原生回归：双窗口、最小宽度/纸面尺寸、键盘与真实中文IME、草稿、透明/迷你、进行中/暂停/到时、结束/休息、跨午夜、手动完成/撤销、个人Markdown外部编辑、断线/原请求重试/版本冲突、完整进程重启。明确区分真实经过午夜与合成时间边界；不能用JS输入冒充IME。没有实际验证的项目继续列待验收。
7. 对新增字段做最后迁移/失败恢复/回退核对，更新 [data-recovery.md](data-recovery.md)、使用说明、状态、台账和报告。不会在正式库上“顺手测试”。
8. 必要检查通过后由集成任务确定版本、提交并构建 release，核对版本/构建提交/SHA-256，按安全退出与备份流程更新本目录 `app/Inky Paper.exe`。没有实际交付就不写“已更新”。

## 本轮不做

不加入P2，不发布远端版本，不自动发起Coach，不新增完整状态覆盖接口，不将已完成的M1—M4推倒重来。真实验证与最终交付均等待用户后续确认。
