# Hermes 卡片显示修复与紧凑布局

适用版本：Inky Paper 0.5.0；Inky Coach 插件 0.1.1。

## 结果

Hermes 用户原会话中的批次 `6923cda4-e844-4d58-b71f-7e692ea19667` 已显示六张真实交互卡片，全部仍是“候选 · 未加入”。本轮未选择、编辑或采用用户的正式计划。

原先后台的“智能体插件 → inky-coach”已启用，但“桌面插件 → Inky Coach”关闭。后台保存候选成功，不等于桌面渲染器已启用；这时 Hermes 将 `::inky-plan{batchId="…"}` 显示为原文。已在实际桌面重新扫描并启用桌面插件，原会话随即读回已有批次，无需再调用模型生成计划。

## 卡片交互

- 双列圆角卡片；窄窗口改为单列。每张显示关联任务、动作、预期结果、首轮分钟和当前状态。
- 点击卡片选择，右上角铅笔打开编辑区，修改动作、预期结果与时长。上下箭头调整顺序。
- 底部显示已选标签与首轮合计分钟，点标签可取消选择；点击“加入 Inky”才提交。
- 保留原有版本冲突核对、断连原请求重试与完成状态同步。定期刷新仅读取数据，不调用 Coach，不启动计时。

截图参考仅用于卡片布局、编辑入口和选择反馈；未新增习惯打卡、连续天数或没有事实依据的进度条。

## 验证

- 插件自动测试 6 项通过，覆盖选择与编辑、顺序、提交、版本冲突、未知提交结果重试、完成后取消勾选及草稿不写入计划；JavaScript 语法检查通过。
- 在独立 Hermes 桌面与 Paper 测试数据库中显示六张卡片，选择两张、修改动作与首轮时长、通过标签取消再选、调整顺序并点击加入。Paper 只增加所选两张，动作、15 分钟时长与排序一致；采用前日计划为空。
- 桌面验证结果：`output/card-fix-20260914/ui-acceptance.json`。该测试使用离线模型配置，没有再次运行模型。
- 正式桌面只验证已有批次的显示与读取，未代替使用者验收真实计划采用或后续工作。
- 正式原会话截图：`output/render-fix-20260914/production-hermes-six-cards.png`；开关与六张未采用卡片的核查结果：同目录 `production-ui-verification.json`。隔离测试实例已退出，正式 Hermes 和 Paper 保持运行。
- 正式插件 JS 与本项目源码 SHA-256 相同。`app/Inky Paper.exe` 保持原 0.5.0，SHA-256 为 `BF29619C6543CBC6022336E359E65CAE5BE15B7F0CE5CEA9AE9C30B193E70BD9`；本轮仅改插件，无需重建 Paper。

## 交付与回退

- 已更新：`C:\Users\31009\.hermes\plugins\inky-coach` 的桌面 JS、插件版本、dashboard manifest 与 README。
- 更新前完整插件备份：`output/card-fix-20260914/before/installed-inky-coach-0.1.0`。
- 安装包：`output/card-fix-20260914/inky-coach-hermes-plugin-0.1.1.zip`，同目录有 SHA-256 文件。包内只有七个运行/说明文件，不含配置、测试数据库或连接凭据。
- 回退时恢复备份中的插件文件并在 Hermes 重新扫描；不需要恢复或覆盖 Paper 数据库。

使用者只需在原 Hermes 会话点一张卡片，观察选中背景与底部标签，再按自己的意愿编辑和加入 Inky。其余人工检查仍见 `docs/人工验收清单-0.5.0.md`。
