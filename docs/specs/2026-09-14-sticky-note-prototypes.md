# 便签与任务清单：A3 已确认

状态：用户已于 2026-09-14 确认 A3（“没问题了，就使用这版”），按该版实现 Inky Paper 0.5.2。下文保留原型选择记录；实现与验收见 `2026-09-14-a3-implementation.md`。

## 已明确的交互

1. 上方恢复贴在纸上的小便签：缩小尺寸、内容居中，保留任务名、短动作、时长、start 和铅笔编辑。
2. 步骤正文可阅读，点击正文不切换当前卡片。通过独立“选用”按钮将它放进上方便签，再点 start 才计时。简单任务可以不展示多步结构，直接选用任务本身。
3. 支持“整个任务完成了”按钮和鼠标横向划过任务标题两种完成方式。完成后原位打勾划线，不移入单独分组；允许撤销。展开、收起和点击选用不能误触发划掉。
4. Hermes 新候选使用短标题；保留实际对象或数量，必要细节用简短预期结果补充，不强行拆解简单事项。

## 三种视觉方向

- A：浅绿便签＋顶部胶带，最接近原有贴纸感。`output/note-prototypes-20260914/A-sage-tape.png`
- B：暖白便签＋两角胶贴，更素净。`output/note-prototypes-20260914/B-ivory-corners.png`
- C：淡黄窄便签＋轻微撕边，便签更醒目。`output/note-prototypes-20260914/C-yellow-note.png`

### A2 调整要求

在 A 绿色胶带便签上继续：卡片相对纸页加宽、左右留白对称，标题保持居中；时间下拉放在左侧、start 放在右侧，同一行垂直对齐，整组居中。保留胶带、铅笔和贴纸阴影，便签高度随横向操作行适当收紧。下方任务交互保持已确认的方向。

A2 原型已生成并核对：`output/note-prototypes-20260914/A2-wide-sage-note.png`。本次只更新原型，等待视觉确认后实现应用界面。

### A3 调整要求

用户反馈 A2 比例仍不协调。新的调整是把便签从接近横条的比例收回到约 2:1：宽度约占纸页 79%，适度增加高度，保持宽于最初 A 版。标题稍收小，按钮与时间下拉视觉平衡、同行居中；便签下沿与“任务”标题之间的空白缩小，阴影和卷角更轻。任务内容和已明确的交互保持不变。

A3 最终原型：`output/note-prototypes-20260914/A3-balanced-sage-note.png`。已检查便签约为 2:1、左右留白对称、控制项仍并排，任务区间距收紧。此图替代 A2，用户已批准作为实现参考。

以上是 imagegen 风格原型，重点比较便签材质、居中布局和任务在原位划线。实际实现应适配当前小窗口与清楚的中文字体；示意图里的系统装饰、额外留白不作为新增要求。

## 已完成的独立工作：Coach 文案

`integrations/inky-coach-hermes-plugin/skills/coach/SKILL.md` 已加入大任务短名称、动作通常 4–14 个汉字、必要时最多 20 字、预期结果通常 20 字以内的提示。只限制生成风格，不机械截断用户内容。简单事项只需一张候选；卡片外避免重复罗列所有内容。

`integrations/inky-paper-mcp-server/index.mjs` 的服务器说明、候选工具说明与字段说明同步添加同一约定，未更改工具权限或存储上限。插件源文件与本机已安装技能同步，版本 0.1.2。已有会话若缓存了旧技能，需要重新读取 `inky-coach:coach`；MCP 新说明在重新建立连接时加载。

JavaScript 语法检查及现有 MCP 合同测试通过：12 个工具、采用边界、幂等重试、日记录版本和连接隔离保持原状。本轮没有运行模型生成新计划，因此没有将提示规则当作实际输出质量验收；未自动改写现有计划。

包：`output/note-prototypes-20260914/inky-coach-hermes-plugin-0.1.2.zip`，SHA-256 `0889a845e833012e2c343356b5d102bd31d2f6b8851dd34f1283c49ac75f167e`。变更前文件在同目录 `before`。
