# 0.6.4 纸面与工作台集成

2026-09-16，合并纸面分支 `2d0d8a3` 与工作台分支 `1dfeef6`，产品代码提交为 `56a4eb6`。版本统一为 0.6.4。

## 交付内容

纸边和底部留白固定可拖动，滚动条为 3px 浮动滑块且不占布局宽度。保留工作台日期、讨论范围、直接选择步骤回 Inky、每日记录与原始 Markdown，以及此前字体与番茄钟缩放。合并时产品代码无冲突；三份说明文档分别保留两边内容。

## 本轮检查

- 类型检查通过；13 个前端测试文件、122 项通过。
- Rust 70 项通过，1 项历史负载基准忽略。
- debug 与 release 构建通过。两套原生测试使用本 worktree 的 debug 文件、独立应用标识与全新数据目录，分别运行并在验收后关闭；没有使用正式工作数据。
- [纸面桌面结果](paper-verification.json)：顶部 / 中部 / 底部拖动、零占位滚动条、鼠标与键盘、大小任务横划、记录输入和保存、透明番茄钟、迷你宠物与固定页脚。
- [工作台桌面结果](workbench-verification.json)：工作台选步骤回 Inky 且不启动计时，Paper 浮层不影响工作台；跨窗口完成与撤销、跨日和未安排工作、工作时段记录、原始 Markdown、外部个人笔记修改、空白日期、1060px 布局，以及结束页完成当前步骤后的回流。

本次未调用真实 Hermes 模型；相关日期与模型调用的历史验证见 [0.6.3](../0.6.3/README.md)。未扩大为全部工作台视觉或所有用户历史数据的验收。

## 正式程序与 GitHub

release 已同步到当前 worktree 和桌面快捷方式对应的 `C:/Users/31009/clauddd/Inky-Paper/app/Inky Paper.exe`，版本及 SHA-256 与构建产物一致。两份原文件分别备份。桌面快捷方式保持原目标，正式 0.6.4 已启动；启动时清除本次进程环境中的调试数据和 WebView 参数。

详见 [交付校验](release-verification.json) 与 [正式进程和快捷方式](desktop-startup.json)。源码进入 GitHub `main`，可执行文件与 SHA-256 校验文件单独位于 [v0.6.4 Release](https://github.com/Xuzheee/Inky-Paper/releases/tag/v0.6.4)。数据库、工作记录和连接凭据继续留在本机，不入源码仓库或 Release。

按用户要求将仓库改为公开。更改前检查所有本地引用可达的 18 个提交、587 个文件对象，其中 436 个文本对象；所检查的凭据模式和禁止入库的个人数据文件名均无命中，见 [发布前检查](publication-audit.json)。这是模式检查结果，不代替任意格式凭据的绝对保证。

截图：[纸面列表](paper-home.png)、[结束页展开记录](end-page.png)、[工作台每日记录](workbench-records.png)。
