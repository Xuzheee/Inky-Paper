Inky Paper 0.6.4 合并纸面与工作台的最新更新。

- 滚动页面后，仍可从纸张左边缘和底部留白拖动窗口。
- 右侧采用不占内容宽度的细浮动滚动条，滚动或悬停时显示，闲置淡出。
- 保留工作台按日期安排、明确讨论范围、选择步骤回到 Inky，以及每日记录和原始 Markdown 同步。
- 保留便签手写字体、缩小后的番茄钟、结束页完成选择和独立操作提醒。

验证：122 项前端测试、70 项 Rust 测试通过，1 项历史负载基准忽略；纸面与工作台两套隔离 Tauri 桌面流程通过。本次未重新调用 Hermes 模型。

Windows 可执行文件见附件，SHA256SUMS.txt 可用于校验。工作台 Coach 需要本机已安装并配置 Hermes 与 Node.js。源码与验证说明在仓库中，个人工作记录不包含在发布包内。
