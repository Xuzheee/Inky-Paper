# C-04 可纠正的偏好

偏好按长期、指定日期、指定项目保存，需用户明确确认。支持编辑、停用、删除，当前请求以最新适用集合为准，空集合明确阻止继续沿用旧对话偏好；当前原话优先。删除仅停止后续 Inky 注入，不宣称已删除旧对话/事件或 Hermes 独立历史。

项目归档后不注入项目偏好；既有偏好仍可停用，不能新建或启用。只复用 Hermes 模型/账户，未增加通用记忆提供方。read_events 明示旧事件不是当前偏好。

28 项上下文事务测试通过（新增偏好9项），9 项请求快照测试通过（含改正/删除后立刻刷新），3 项偏好UI测试通过，类型检查通过。界面入口和四组真实模型对比待 M4。
