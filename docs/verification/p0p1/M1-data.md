# M1 · D-01 数据与接口

M0 基线提交 `e1196ce`。本包实现可空截止日期、安排处理状态、持久化准备来源、安排 before/after 记录、版本化继续/取消及共享查询。

本轮 `corepack pnpm typecheck` 通过，前端 131 项通过，Rust 80 项通过、1 项历史负载基准忽略。新增查询 9 项；数据事务 4 项；迁移 6 项。涵盖旧格式 WAL 备份/还原、升级后新记录保留、高版本拒绝、版本冲突原子回滚、重复继续复用目标、完整取消范围校验、既有会话来源不变、跨重启准备来源和 Markdown 截止字段。

输入全部为合成夹具、内存 SQLite 或独立 tempfile；未读取正式数据库。前端尚未接入新队列与遗留操作。本包自动检查通过，M1 原生桌面流程及模型验证均未进行；不代表 M1 放行。
