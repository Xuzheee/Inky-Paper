# M1 验收

2026-09-16，在 `codex/inky-workbench` 的独立调试应用完成。产品代码截至 `6e71b41`，应用标识 `com.inky.paper.dev.workbench`，数据位于 `output/p0p1-m1-native-02/paper-test`，未读取正式数据库。

## 结果

- 166 项前端检查通过；82 项 Rust 检查通过（1 项历史负载基准忽略），类型检查和调试构建通过。Rust/专项记录见同目录上级工作包报告。
- 实际 Tauri/WebView2：旧库启动备份、旧会话快照不变、今日双步骤、完成/撤销原位保留、父任务独立、遗留继续与完整取消预览、字段编辑均通过。
- 交接只准备；显式 start 才计时；返回任务列表继续计时；进行中拒绝换步。未来安排提前执行保留原计划日，实际计时归真实当天，空反馈可以保存。
- 共同命名重排后实际 Paper 队列同步；取消未执行安排时保留已执行来源并提示剩余有效安排。
- 实际每日记录与原始 Markdown 可见；外部编辑个人笔记后再次导出任务，个人笔记不被覆盖。
- 完整进程重启后恢复 taskId/stepId/dayItemId，保留来源日，没有自动计时。截图及 JSON 见本目录。

## 脚本与证据限制

`verify-p0p1-m1.mjs` 用于新合成旧库，`verify-p0p1-m1-records.mjs` 补充记录检查，`verify-p0p1-m1-restart.mjs` 用于完整进程重启。首次脚本使用未支持的 focus 导航参数，改为真实“返回番茄钟”按钮后在新实例完整通过；记录脚本的文件选择器和版本参数也已修正。补充记录脚本采用 `records-only` 续跑，前两项成功结果由终端输出核对，本报告保留该执行方式，不声称三脚本一次无修改通过。

M1 阶段放行；真实模型尚未调用，随 M2 验收。最小宽度、中文输入法、跨午夜等完整回归仍归 M5，当前不声称整轮 P0/P1 已完成。尚未更新正式 release 可执行文件。
