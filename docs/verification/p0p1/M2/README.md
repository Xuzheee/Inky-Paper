# M2 验收：请求上下文与调整候选

2026-09-16，代码提交 `39d01a7`、`3fd2af9`，独立 Tauri/WebView2、`output/p0p1-m2-native-01` 合成数据。实际模型为 Hermes 配置的 `openai-codex / gpt-5.6-sol`，两次用户请求；没有读写正式 Paper 数据。

- 188 项前端、108 项 Rust、类型检查通过。工具权限测试与实际 ACP `/tools` 探测确认仅 12 个 Paper 读取/提案工具；模型直接改任务、采用、控制时钟均被后端拒绝。
- 第一轮：三个已有步骤，提出 60 分钟优先安排。候选保留高优先级步骤并改为 60 分钟预留，另外两步延期。采用前正式状态不变；采用后原任务/步骤保留，Paper 同步且未计时。流式回复途中切换查看日期，仍绑定原请求范围。
- 第二轮：两个独立改期组。只采用一个只修改对应安排；外部更新另一任务后，旧建议拒绝采用，未产生部分写入；刷新后所选冲突草稿仍在。
- 常驻三个入口只预填，不触发模型。首轮时长仍为 15 分钟，与 60 分钟预留分开。

证据：[第一轮](desktop-model-partial.json)、[实际候选](model-adjustment.json)、[第二轮](partial-adoption-model.json)、[真实工具面](acp-tools.json)。JSON 中的 partial 是脚本分段命名，第二份报告 `complete: true`；本结论仅覆盖 M2。完整评测、最小窗口及 M3—M5 新增流程尚未验收。

初始 UI 定位发生重复标题匹配，修复选择器后继续同一合成状态；没有重复模型调用。ACP 初始探测发现只改 `load_config` 不足以隔离工具/记忆，已同时覆盖只读配置入口并禁止工具搜索聚合，详见 [C-02](../C-02.md)。仓库中的旧 Hermes 插件点击代理已适配独立用户采用凭据，外部已安装插件未改动；工作台原生采用已验证。
