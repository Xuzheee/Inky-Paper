# M3 原生验收

2026-09-16；源码 `2d8561a`，debug 打包界面，Windows Tauri/WebView2。全部使用仓库 `output/p0p1-m3-*` 新建隔离数据库和 WebView profile，没有使用正式记录。前端 255/255、Rust 152/152（另1项历史负载基准按原配置忽略）、typecheck 通过。

## 本轮结果

- `desktop-verification.json`：原生专注中随手记→返回专注→不填反馈保存→工作台转任务→继续同一步骤；保留原文、noteId、会话和动作来源，转换只有一个目标。
- 已完成步骤后的父任务确认只响应显式选择。“保留后续”不完成父任务；另一个进程 PID 重启后不重复提示。准备来源和完整历史会话重启前后相同，没有自动启动下一轮。
- 真实 `workbench_save_step` 提交成功后，测试包装当前 WebView 的 `fetch` 返回一次失败响应；草稿恢复保留原请求，重试只有一个任务。故障注入标志已断言；不是假写入或离线模拟。
- `controls.json`：输入草稿经收起、展开和页面重开仍在；连接检查显示四阶段，未调用模型。批量改期先预览、后采用，保留时刻、预留和既有执行记录。

真实流式回复中收起侧栏的检查随 M4 模型轮次一起完成，单列证据，不用本页的空闲状态检查替代。输入法真实组合输入也留到最终桌面回归。

## 操作成本

旧构建 `3fd2af9` 的可比片段只测了随手记和跳过反馈保存（`baseline-operations.json`）。随手记两次点击+输入、结束保存两次点击，新版相同；保存点击到回首页在本次自动操作样本为 72 ms，旧样本约854 ms。测试环境、调度和样本数不足以判断性能提升，更不能推断用户生产力。没有完整旧循环耗时，未补造对比。

## 脚本调整记录

首次故障注入尝试覆写 Tauri 的只读 `invoke`，注入没有发生，因而该次未通过。改为等待真实 IPC 成功响应后注入一次错误并检查标志，使用新隔离目录完整重跑。批量操作脚本曾使用错误的刷新/关闭可访问名称，修正后在另一新目录完整通过；没有修改产品来迎合错误选择器。旧失败测试目录保留在忽略的 output 中。

复现脚本：`scripts/desktop/verify-p0p1-m3.mjs`、`verify-p0p1-m3-restart.mjs`、`verify-p0p1-m3-controls.mjs`。重启脚本要求不同 PID。
