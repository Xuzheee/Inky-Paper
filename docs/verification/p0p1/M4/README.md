# M4 验证记录

2026-09-16。独立原生实例 `p0p1-m4-native-05` 的六组 UI 检查已通过：未知/0预算、预留与占用区分、重叠/不可用冲突、项目管理与关联筛选、偏好修改停用删除、归档不丢工作。报告 `desktop-verification.json`，页面错误0，模型调用0。

原生 UI 所用源码 `2d8561a`，debug exe SHA-256 `E2F14A1C57DD8ABAFAC2679CEB6E01D9806BD1DBB774404CA35D8EEF066AB0EA`。四次早期脚本未完成记录保留：数据事件等待不足、textarea/select 的精确 label 定位、option 禁用状态判断；核对真实 DOM/后端后修正测试，没有改变产品规则。

## 模型首轮发现与修复

`p0p1-m4-native-05/model-1789558902201/model-context-evidence.json` 保存实际三次发送。第一轮在真实工具读取后返回 `API call failed after 3 retries: Connection error.`；上下文和未采用边界检查虽通过，但这不是有效建议，模型验收不通过。第二轮连接恢复，产生45分钟候选，未采用。集成任务手动取消时第三轮刚开始，因此第三轮是测试人员取消，不能归为侧栏取消或产品失败。

源码检查确认 Hermes 把耗尽重试的错误文本作为 ACP `end_turn` 返回。修复提交 `6e46136` 将这种已知终止错误归为 error，诊断显示模型失败，并用固定说明替代底层文本。专项测试通过。模型验收脚本同时检查回答状态及终止错误，防止把连接失败当作合格回答；随后使用新隔离实例重新验证四种条件。

本页尚不声明 M4 全部验收；四个有效回答需要逐条阅读、确认新时间/项目目标/偏好在实际建议中生效。参考链接已验证不会自动访问；显式系统浏览器打开另列待验。

## 最终复核与放行

最终原生六组在 `p0p1-m4-native-07` 全部重跑通过，源码 `dc2609c`（其后 abddf02 仅契约文档）。`p0p1-m4-native-06/model-1789559780217/` 的四轮回答及结构化候选已逐条检查：前两轮没有虚构总预算/未估计工时；后两轮虽反映项目目标和5分钟偏好，但出现当天已过去的钟点，因此没有直接放行。

补充真实 `currentLocalTime` 与后续安排规则后，`p0p1-m4-native-07/model-1789560300945/` 针对后两轮重跑：候选不再使用过去钟点；先核对甲结论，再补乙材料；5分钟偏好真实进入新增步骤 `plannedSeconds=300`，原完整工作保留，未知总工时为 null。没有自动采用、时钟或正式计划变化。M4按“06前两轮+07纠正后两轮”放行，保留全部失败/纠正前样本，不宣称固定模型永不出错。

M3流式折叠也由06第一轮实际验证：相同requestId、正在回复的窄条、展开后完成、没有取消命令。四阶段诊断与输入草稿检查见M3。

本轮实际提供方为 Hermes 只读配置 `openai-codex / gpt-5.6-sol`。仅提取名称与非敏感设置；reasoning_effort/temperature未显式设置，不推断有效值。可投入未知时的简短回答没有专门重述“预算未知”，仍有文案改善空间，但没有声称整体任务已适配或完成。

待手工项：真正中文输入法组合输入；显式参考链接启动系统浏览器。P0/P1完整交付仍需M5总结、固定套件与最终回归。
