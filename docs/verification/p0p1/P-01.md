# P-01 · Paper 今日队列

默认首页采用公共 `planningViews` 的今日步骤队列。同一父任务多个步骤分别按日顺序显示，完成原位保留；正文展开，Do this 才选择，横划和勾选仅完成步骤。保留“全部任务”和“临时做一件”；空日可展开未安排事项。DayPlan 与首页日计划计数共用相同查询。

专项 `TaskSheet.test.tsx`、`PaperApp.test.tsx`、`DayPlan.test.tsx` 共 87 项通过，类型检查通过。测试验证同父多个步骤、完成和撤销、父任务独立、队列排序、空日、未安排与现有计时流程。

未完成原生桌面验收。持久化准备来源和默认便签选择由后续 P-02 处理；本包不代表 M1 放行。
