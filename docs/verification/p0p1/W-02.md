# W-02 · 跨界面字段一致

工作台编辑器提供共用任务优先级、截止备注、可选独立截止日期和步骤完成标准。卡片简显优先级/截止日，展开后及每日记录展示当前完整字段；计划日期与截止日期分别保存。未改字段省略提交，用户主动清空才传 null；冲突核对展示最新共用值。

`StepEditor.test.tsx` 5 项通过；类型检查通过。覆盖旧值保留、独立截止日期、编辑/清空和完整标准展示。后端完成标准上限与原候选工具统一为 2000 字。D-01 新代码本次仅做 rustfmt 排版，无其他数据语义变化。

尚未原生桌面验收；本工作包未调用真实模型。合成单元测试不作为 M1 交付验收。
