# W-05 A：快速添加与恢复

新建初始只要求标题，其余字段按需展开。对象草稿保存稳定对象 ID、原版本及修改字段；关闭重开可恢复。源对象变化后保留草稿并要求明确核对，未修改字段使用最新值，不复活已删除对象。

命名操作在 IPC 前按当前数据目录 scope 保存完整请求；未知结果从原请求核实，重启后不另造对象。存储未就绪/失败不发送修改。

28 项专项和类型检查通过，覆盖新任务丢响应、重启、跨目录隔离、冲突和存储故障。原生恢复流程待 M3。C02 旧卡片草稿的已有存储本包未迁移。
