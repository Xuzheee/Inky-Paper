# 滚动后的拖动与浮动滚动条

2026-09-16，`codex/markdown-coach`，当前 worktree。首页、结束页及其他纸面页面沿用现有排版。

## 改动

- 左侧 10px 纸边、底部 6px 留白固定可拖动，页眉滚走后仍能移动窗口；原有结束页 / 番茄钟纸面拖动保留。
- 主滚动区不再预留原生滚动条宽度。右侧滑块宽 3px、透明操作区宽 10px，滚动 / 悬停 / 键盘聚焦显示，闲置淡出，无溢出时不显示。
- 滚动层与拖窗层独立，支持滚轮、拖动、轨道翻页和键盘；任务横划、按钮、输入框保持原行为。透明番茄钟不新增底色，迷你宠物卸载这层控件。

## 本轮验证

`corepack pnpm typecheck`、`corepack pnpm test:frontend`（9 文件 / 114 项）、`git diff --check`、debug 和 release 构建通过。新增的 10 项滚动条测试覆盖溢出变化、键盘、拖动、缩放、滚轮、取消与卸载。本轮没有 Rust 源码和数据操作变更，未重跑 Rust 全套或调用 Hermes 模型。

独立 Tauri / WebView2 验证在 `com.inky.paper.dev.execution`、9252 端口与全新测试数据中运行。详见 [桌面结果](desktop-verification.json)：

- 列表顶部 / 中部 / 底部均验证左边缘与底部拖动的真实原生坐标变化；拖动和滚动不修改任务、计划或会话。
- 首页与展开记录页实际滚动条占位为 0px，滑块为 3px、操作区为 10px；拖滑块、滚轮和键盘均不移动窗口，文字宽度不跳动。
- 大小任务横划分别完成、原位保留；补充记录输入、选项和保存正常；切换页面后滚动条跟随正确容器。
- 透明态计时位置保持、浮层没有底色；迷你宠物移除浮层，返回后原会话继续；滚动后的首页宠物与英文标语仍可见。

复验脚本等待原生窗口完成尺寸切换、激活测试页，并按会话 ID 核对第二次计时，避免布局变化或读取第一条历史会话导致误判。运行方式见 [脚本说明](../../../scripts/README.md)。

## 截图与交付

- [滚动条显示](home-scrollbar.png)
- [闲置淡出与固定页脚](home-idle.png)
- [展开补充记录](end-notes.png)
- [番茄钟透明态](focus-transparent.png)

release 已同步到当前 worktree 的 `app/Inky Paper.exe`，版本 0.6.1；与构建产物 SHA-256 一致，旧文件已备份，见 [交付校验](release-verification.json)。按照并行开发约定，桌面正式程序位于另一 worktree，本轮没有覆盖；合并纸面与工作台分支后统一验收、更新。

测试目录由 Git 排除，不收录测试数据库、连接文件或 WebView 用户配置。上述记录为自动化验证，人工仍可按 [检查清单](../../MANUAL_CHECKS.md) 体验拖动与细滚动条手感。
