{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Inky window",
  "windows": ["main", "coach-prompt"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
